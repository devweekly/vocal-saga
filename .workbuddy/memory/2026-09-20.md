# 2026-09-20

## 提交（3 个 commit，按逻辑分组）

- `f7f7315` feat: 识别 `data-as="p"` 段落声明（Mintlify 系文档站）
  - `lib/translate/blockExtractor/rules.ts` 新增 `PARAGRAPH_LIKE_ATTRS`
  - 修复 docs.langchain.com 类站点「标题翻译了、正文仍是英文」的根因：正文根是 `<main>` 而 `isInsideArticle()` 只认 `role="main"`
- `fc6af32` feat: AWS Blogs 站点规则（新增 `lib/translate/rules/awsblogs-rules.ts` + 注册到 `rules/index.ts`）
  - 隐藏两个 `position:fixed` 浮层：`#m-subnav` 子导航条、`[data-testid="feedback-button"]`
- `64cf5fe` chore: 删除 deepseek 测试里整段注释掉的死代码

提交前验证：`tests/blockExtractor.test.ts` + `tests/displayRules.test.ts` 共 311 测试全过。

## 环境备注

- `.git/index.lock` 出现过陈旧锁（残留 >24h，无 git 进程在跑）。标准处理：`pgrep -x git` 确认无进程后 `rm -f .git/index.lock`。
- `.workbuddy-ai/memory/` 是本地 AI 记忆目录，未纳入本次提交（保持 untracked）。
- 本机 npx 路径已变更：用 `/Users/saga/.workbuddy/binaries/node/versions/22.22.2-3/bin/node`（旧 `22.22.2-2` 已不存在），或直接 `./node_modules/.bin/vitest`。
